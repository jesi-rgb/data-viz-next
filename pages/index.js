import Head from "next/head";
import CalendarHeatMap from "@/components/CalendarHeatMap";
import styles from "../styles/Home.module.css";
import YoutubeViewsCount from "@/components/YoutubeViewsCount";
import Footer from "@/components/Footer";
import useSWR from "swr";
import fetcher from "@/lib/fetcher";

export default function Home() {
  const { tweets } = useSWR("/api/tweets", fetcher);
  const { youtubeData } = useSWR("/api/yt-video-plays", fetcher);
  console.log(youtubeData);

  return (
    <div className="container">
      <Head>
        <title>Create Next App</title>
        <meta name="description" content="Generated by create next app" />
        <link rel="icon" href="/favicon.ico" />
      </Head>

      <YoutubeViewsCount data={youtubeData} />

      {/* <CalendarHeatMap data={tweets} /> */}

      <Footer />
    </div>
  );
}
